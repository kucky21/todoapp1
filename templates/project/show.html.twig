{% extends 'template.html.twig' %}

{% block title %}Projet{% endblock %}

{% block main %}
    <h1>Projet N° {{ project.id }}</h1>
    <section class="row text-center">
       	<article class="table txt maincolor container p-4 m-4 col-lg-5 col-sm-10 align-items-center justify-content-center">
				<h4 class="card-header m-2 bg-white">{{ project.label }}</h4>
				<ul class="list-group list-group-flush align-items-center">
					<li class="list-group-item my-2"> Opened {{ project.openingDate ? project.openingDate|date('d-m-Y') : '' }} until {{ project.deadline ? project.deadline|date('d-m-Y') : '' }}</li>
					<li class="list-group-item my-2">{{ project.description }}</li>
				</ul>
			<div class="container row justify-content-around my-2">
				<a class="btn bodycolor txtheader p-2 m-1" href="{{ path('project_index') }}">Return</a>
				<a class="btn btn-success" href="{{ path('project_add_task',{'id': project.id, 'project' : project}) }}">Add task</a>
				<a class="btn bodycolor txtheader p-2 m-1" href="{{ path('project_edit', {'id': project.id}) }}">Edit</a>
                {{ include('project/_delete_form.html.twig') }}
			</div>
		</article>
    {% include 'task/tasks.html.twig' %}
	</section>
{% endblock %}